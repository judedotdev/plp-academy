void main() {
  var list1 = [10, 20, 30, 40, 50];
  int index = 0; // Initialize index

  while (index < list1.length) {
    // Loop until index is less than the length of the list
    print(list1[index]); // Print the current element at index index
    index++; // Increment the index
  }
}
